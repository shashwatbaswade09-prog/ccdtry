function showMessage() {
    const messageElement = document.getElementById('message');
    messageElement.textContent = 'is the application running?';
    messageElement.style.animation = 'fadeIn 0.5s ease-in';
}
