function showMessage() {
    const messageElement = document.getElementById('message');
    messageElement.textContent = '🎉 Application is working perfectly!';
    messageElement.style.animation = 'fadeIn 0.5s ease-in';
}
